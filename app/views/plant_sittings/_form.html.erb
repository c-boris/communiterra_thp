<% if plant_sitting %>
  <%= form_with(model: plant_sitting) do |form| %>
    <% if plant_sitting.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(plant_sitting.errors.count, "error") %> prohibited this plant_sitting from being saved:</h2>

        <ul>
          <% plant_sitting.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <!-- Form fields go here -->

    <%= form.submit "Submit" %>
  <% end %>
<% else %>
  <p>No plant sittings available.</p>
<% end %>
